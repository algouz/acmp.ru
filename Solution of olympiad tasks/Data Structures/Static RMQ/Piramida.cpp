#include <iostream>
#include <vector>
using namespace std;

vector<vector<int> > a;

int main(){
    int n, m, a, b, c, d;
    cin >> m >> n >> b >> a >> d >> c;
    vector<vector<int> > a(n, vector<int>(m));
}